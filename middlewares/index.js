const authenticate = require("./authenticate");
const upload = require("./upload");
const avatarProcessing = require("./avatarProcessing");

module.exports = { authenticate, upload, avatarProcessing };
